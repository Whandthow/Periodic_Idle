-- Автосинтез елементів (Тір 2) і молекул (Тір 3) — щоб гравець не клікав
-- вручну кожен синтез, коли частинок/атомів вже достатньо. Вимкнено за
-- замовчуванням (свідомий вибір гравця, не сюрприз для нових saves).
ALTER TABLE saves ADD COLUMN auto_synthesize_enabled BOOLEAN NOT NULL DEFAULT FALSE;
