-- Ребаланс під ціль "3 дні активної гри до 1-го атома" (жива калібрація,
-- docs/balance.md V19). Крок 1 з калібрування: фікс блокуючої проблеми,
-- без якої гравець фізично не може дістатись Тіру 1 після 1-го престижу.
--
-- tier_unlock_conditions для tier=1 перевіряв лише E>=1e308 АБО вже
-- наявний p/n/e>0 — але отримати перший p/n/e можна ЛИШЕ через кнопку
-- "Обмін" (ExchangeService), яка сама живе на сторінці Тіру 1. Гравець,
-- щойно отримавши перші Кристали Пустоти (VC) з престижу, не мав
-- жодного видимого шляху далі. Додаємо VC як ще одну OR-умову.
INSERT INTO tier_unlock_conditions (tier, resource_id, min_log10)
SELECT 1, id, 0 FROM resources WHERE code = 'VC';
